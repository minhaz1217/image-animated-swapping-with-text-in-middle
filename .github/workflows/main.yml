name: Deploy

on: [push]

jobs:  
    build:
        name: Build
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v1
        
        - name: multiple command
          uses: appleboy/ssh-action@v0.1.7
          with:
            host: ${{ secrets.HOST }}
            username: ${{ secrets.USERNAME }}
            key: ${{ secrets.SSH_KEY_1 }}
            port: ${{ secrets.PORT }}
            script: |
              cd ~/github-action
              git clone https://github.com/minhaz1217/image-animated-swapping-with-text-in-middle.git
              cd image-animated-swapping-with-text-in-middle
              sudo docker build -t ia-image-animated-swapping-with-text-in-middle . <<< "${{ secrets.MYSUCCESS }}"
              sudo docker run -dit --name image-animated-swapping-with-text-in-middle --network ${{ secrets.DOCKER_NETWORK }} ia-image-animated-swapping-with-text-in-middle  <<< "${{ secrets.MYSUCCESS }}"
              sudo docker ps  <<< "${{ secrets.MYSUCCESS }}"